allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}

// buildscript {
//     ext.kotlin_version = '2.0.21' // Update this to the latest version
//     repositories {
//         google()
//         mavenCentral()
//     }
//     dependencies {
//         classpath "com.android.tools.build:gradle:8.1.0" // Make sure you have the latest Gradle plugin
//         classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
//     }
// }

